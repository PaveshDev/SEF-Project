# Web client

Minimal React/Vite application. See [setup](../docs/setup.md), [ownership](../docs/ownership.md), and [dependency policy](../docs/dependency-policy.md). Run `npm ci`, then `npm run dev`. Only the skeleton home page exists.
