# Recovery

Member 2 owns this feature and `mobile/test/recovery/`. Add routes to `recoveryRoutes` in routes.dart; the central router already includes the list. Use the shared Dio client for future ASP.NET requests. Empty ownership placeholder; see [ownership](../../../../docs/ownership.md).
