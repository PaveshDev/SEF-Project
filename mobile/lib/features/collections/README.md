# Collections

Member 4 owns this feature and `mobile/test/collections/`. Add routes to `collectionsRoutes` in routes.dart; the central router already includes the list. Use the shared Dio client for future ASP.NET requests. Empty ownership placeholder; see [ownership](../../../../docs/ownership.md).
