# Partners

Member 3 owns this feature and `mobile/test/partners/`. Add routes to `partnersRoutes` in routes.dart; the central router already includes the list. Use the shared Dio client for future ASP.NET requests. Empty ownership placeholder; see [ownership](../../../../docs/ownership.md).
