# Items

Member 1 owns this feature and `mobile/test/items/`. Add routes to `itemsRoutes` in routes.dart; the central router already includes the list. Use the shared Dio client for future ASP.NET requests. Empty ownership placeholder; see [ownership](../../../../docs/ownership.md).
