# Reserved agent area

Only ownership documentation and empty `shared/contracts/` and `shared/runtime/` folders exist. No framework has been selected or installed. There is no agent service, implementation, prompt, tool definition, orchestration, or endpoint.

Members own their corresponding module folders. Future integration contracts and shared runtime changes require a coordinated integration change. Future clients continue to communicate through ASP.NET, never directly with an AI service.
