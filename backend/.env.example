# Reference only: ASP.NET does NOT automatically load .env files.
# Set environment variables or use dotnet user-secrets; see docs/setup.md.
# Not needed for /health. Replace placeholders only in private local configuration.
ConnectionStrings__DefaultConnection=Host=<NEON_HOST>;Database=<DATABASE>;Username=<USER>;Password=<PASSWORD>;SSL Mode=VerifyFull
