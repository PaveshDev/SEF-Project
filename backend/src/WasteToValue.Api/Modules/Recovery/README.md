# Recovery

Member 2 owns this module and `backend/tests/Recovery/`. The empty `AddRecoveryModule` extension is already called in Program.cs. Future entity configurations are discovered through AppDbContext's assembly scan. Do not create another DbContext or migration directory. No business implementation exists. See [ownership](../../../../../docs/ownership.md) and [migration policy](../../../../../docs/migration-policy.md).
